image: "https://hub.docker.com/_/node/:latest"

stages:
  - ver
  - init
  - tests

ver-job: 
  stage: ver
  script:
    - "yarn --version"

client-init-job: 
  stage: init
  script:
    - "cd client/"
    - "yarm cache clean"
    - "rm -rf node-modules"
    - "yarn install"

server-init-job:
  stage: init
  script:
    - "cd server/"
    - "yarn cache clean"
    - "rm -rf node-modules"
    - "yarn install"

client_run_tests: 
  stage: tests
  script: 
    - "cd client/"
    - "yarn test"

server_run_tests: 
  stage: tests
  script: 
    - "cd server/"
    - "yarn test"